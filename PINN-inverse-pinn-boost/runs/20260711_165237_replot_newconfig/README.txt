Replotted 2026-07-11 from trained artifacts in runs/20260702_160102_integral
(the integral-residual PINN run), re-rendered against the EDITED config
(regimes renamed Normal / Early Adenoma / Advanced Adenoma / Severe APC Loss;
updated variable labels). Training was NOT re-run — physics values
(W, thetaP, tau2=88) are unchanged from that run; only labels/references refreshed.
Regenerated via: python3 regen_plots.py <this dir>
