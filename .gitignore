LOGS.md
CLAUDE.md
PINN-inverse-solve/inverse_pinn_architecture.png
__pycache__/
*.aux
PINN-inverse-better/architecture.log

# stray LaTeX build artifacts from root-dir compiles
/build.log
/texput.log

# Local working notes (AGENTS.md is the repository guide; CLAUDE.md is legacy)
notes/
PINN-smaller/forward-pinn-train-hybrid/architecture.log

# macOS finder junk
.DS_Store

# Beamer/LaTeX build output -- only the sources and the final PDF are tracked
presentation/build/
*.nav
*.snm
*.toc
*.out

# presentation-codex scratch: images/text extracted from the source PDF
presentation-codex/tmp/
presentation-codex/output/latex/*.log

# research-paper build output -- .tex/.bib/figures and the final PDF are tracked
research-paper/*.log
research-paper/*.blg
research-paper/*.bbl

# example posters (reference material, not tracked)
examples-posters/

# Package builds and local environments
*.egg-info/
/build/
/dist/
/.venv/
/.pytest_cache/
/.ruff_cache/
*.py[cod]
.ipynb_checkpoints/

# New experiment outputs and local artifact stores
/runs/
/.artifact-store/
/artifacts/receipts/
/poster-versions/
/poster-fable/
/poster-fable.zip

# Historical run artifacts are indexed in artifacts/catalog.json.
# Existing baseline files remain tracked; ignored does not remove tracked files.
**/runs/
!/src/wnt_pinn/runs/
!/src/wnt_pinn/runs/*.py

# The ecology project has its own repository; retain the migration pointer.
/new-theme/*
!/new-theme/README.md
