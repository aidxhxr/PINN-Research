{
  "name": "kernel-smoke-full",
  "pipeline": "hybrid",
  "regimes": ["Normal"],
  "data": {"T": 150.0, "reference_points": 1000, "n_data": 150},
  "training": {
    "adam_epochs": 60,
    "lbfgs_steps": 2,
    "param_refine_steps": 1,
    "n_colloc": 8000,
    "param_refine_colloc": 8000,
    "n_starts": 1,
    "weight_every": 20,
    "log_every": 20,
    "checkpoint_every": 60,
    "physics_backend": "triton"
  },
  "hybrid": {"term": "bm_myc"},
  "resources": {"device": "cuda", "threads": 4, "concurrency": 1}
}
